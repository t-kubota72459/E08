# 電子工学概論 2026　第2回 実習準備

# ブレッドボードとロジックICの使い方
## ― 初めての弱電回路実習 ―

---

## 今日の実習で一番大切なこと

今日は、回路を速く作ることよりも、

> **正しく配線し、電源を入れる前に確認する習慣をつける**

ことを優先する。

回路を変更するときは、必ず電源をOFFにする。

---

## 今回使うロジックIC

第2回では、次の **74HCシリーズ** のICを使う。

| 論理 | 使用IC | 中に入っている回路 |
|---|---|---:|
| NOT | 74HC04 | NOT × 6 |
| AND | 74HC08 | 2入力AND × 4 |
| OR | 74HC32 | 2入力OR × 4 |
| XOR | 74HC86 | 2入力XOR × 4 |

今回はすべて同じ74HCシリーズなので、電源の扱い方や実習の基本手順をそろえることができる。

型番そのものを暗記する必要はない。

> **「このICの中には、何の論理ゲートが何個入っているか」**

を確認して使おう。

---

# 1. ブレッドボードの中はどうつながっている？

ブレッドボードは、はんだ付けをしなくても部品を差し込んで回路を作れる基板である。

しかし、すべての穴がつながっているわけではない。

## 導通を調べる

テスターの導通確認を使って、自分のブレッドボードの内部接続を確認する。

### 手順

1. 電池はまだ接続しない
2. テスターを導通確認にする
3. 同じ列の近い穴を2か所測る
4. 少し離れた穴も測る
5. 中央の溝をまたいで測る
6. 電源レールも測る

### 確認

- [ ] 同じグループの穴が内部でつながっていることが分かった
- [ ] 中央の溝をまたぐとつながっていないことを確認した
- [ ] 電源レールのつながり方を確認した

> 製品によっては、電源レールが途中で分かれている場合がある。  
> 自分のブレッドボードは、**測って確認する**。

---

# 2. 電源

今回の電源には、単3形充電池4本の電池ボックスを使う。

```text
赤線 → ＋側（VCC）
黒線 → －側（GND）
```

ロジックICは、このVCCとGNDを基準にして0と1を扱う。

この実習では、おおまかに

```text
GND付近 → 0（LOW）
VCC付近 → 1（HIGH）
```

として扱う。

---

# 3. ICの向きとピン番号

DIP型ICには向きがある。

切り欠きや●印を確認して、上から見たときに1番ピンを見つける。

```text
        切り欠き
       ┌──────┐
   1  ●│      │ 14
   2   │      │ 13
   3   │      │ 12
   4   │      │ 11
   5   │      │ 10
   6   │      │  9
   7   │      │  8
       └──────┘
```

ピン番号は、1番ピンから **反時計回り** に数える。

今回使う14ピンのICでは、基本的に

```text
14番ピン → VCC
 7番ピン → GND
```

である。

※必ず、そのICのピン配置を資料で確認してから配線する。

---

# 4. ICをブレッドボードへ挿す

ICは、ブレッドボード中央の溝をまたぐように挿す。

### チェック

- [ ] ICの向きを確認した
- [ ] 中央の溝をまたいで挿した
- [ ] 足が曲がっていない
- [ ] VCCとGNDのピン番号を確認した

---

# 5. 電源を安定させるコンデンサ

ICのVCCとGNDの近くに **0.1 µFのセラミックコンデンサ**を1個入れる。

```text
VCC ──||── GND
      0.1µF
```

今日は詳しい仕組みまでは扱わない。

> **ICの電源を安定させるために、ICの近くへ入れる**

と覚えておこう。

---

# 6. LEDには抵抗を入れる

LEDを直接電源やIC出力へ接続してはいけない。

必ず電流を制限する抵抗を入れる。

```text
出力 ── 抵抗 ── LED ── GND
```

LEDには向きがある。
点灯しないときは、配線だけでなくLEDの向きも確認する。

---

# 7. 入力を「どこにもつながない」にしない

CMOSロジックICの入力を、VCCにもGNDにもつながない状態にしない。

この状態を **Floating（フローティング）** という。

今回の入力は、必ず

```text
GNDにつなぐ → 0
VCCにつなぐ → 1
```

のどちらかにする。

また、**使っていない入力端子も放置しない**。
先生の指示どおり、VCCまたはGNDへ接続する。

---

# 8. 電源を入れる前のチェック

電源スイッチをONにする前に、次を確認する。

- [ ] VCCとGNDを逆につないでいない
- [ ] ICの向きが正しい
- [ ] 14番ピンがVCC、7番ピンがGNDになっている
- [ ] LEDに抵抗が入っている
- [ ] 入力がFloatingになっていない
- [ ] VCCとGNDを直接ショートしていない

> **配線を変えるときは電源OFF。確認してからON。**

この手順を毎回同じように行う。
